import SwiftUI

fileprivate struct MGE_GeometryEffectOnParkTitle: View {
    var body: some View {
        // This is a placeholder
        Text("Look for the changes I made the changes directly to the row and detail views.")
    }
}

struct MGE_GeometryEffectOnParkTitle_Previews: PreviewProvider {
    static var previews: some View {
        MGE_GeometryEffectOnParkTitle()
    }
}
